import React, { Component } from "react";
import { ScrollView, Text } from "react-native";

export default class ProjectStudents extends Component {
  render() {
    return (
      <ScrollView>
        <Text>Sum Students n Stuff</Text>
      </ScrollView>
    );
  }
}
